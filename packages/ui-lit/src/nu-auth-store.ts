// Stub auth store for nu-awr-upload-link component
import { signal } from '@lit-labs/signals';

// Mock authentication state - defaults to true for testing
export const isLoggedIn = signal<boolean>(true);